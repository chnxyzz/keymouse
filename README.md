# keymouse
```
Windowsxp python3.4.4
Python -m pip install rabird.winio


Other
Python -m pip install -r req.txt 
python -m pip install -i https://mirrors.aliyun.com/pypi/simple -r req.txt

other two python3.4.4
python -m pip install -i https://mirrors.aliyun.com/pypi/simple pytest-runner
python -m pip install -i https://mirrors.aliyun.com/pypi/simple pywinio
```
